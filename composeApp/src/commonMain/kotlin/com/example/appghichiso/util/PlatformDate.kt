package com.example.appghichiso.util

/** Returns the current year on all KMP platforms. */
expect fun currentYear(): Int

/** Returns the current month number (1–12) on all KMP platforms. */
expect fun currentMonth(): Int

